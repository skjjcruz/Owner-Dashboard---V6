# Independent actual-public Trade Center pick review — 2026-09-20

Disposition: the bounded ownership adapter and its corrected evaluation gates have no unresolved material finding from this review. This is not a Trade Center or suite readiness approval. The consumer still requires a coordinated canonical shared release, and seasonal/startup pricing, global assessment inputs, full deal generation/save/reopen and live provider evidence remain open.

## Reviewed source and evidence

Reviewed actual-public `cd85e24` from `80933bb` in the isolated Trade Center worktree, then its source-owner follow-up (final revision recorded below). Canonical test dependency was `dhq-shared-readiness-actual-capital`, whose runtime sources at `4d6ef17` are unchanged by later report-only commits. The canonical consumer pin was not changed. Reviewer made no product source edits or remote requests/mutations.

Independently executed the initial 19 actual shared/provider/caller groups and the corrected 22 groups using Node 20.20.2. [Final focused log](evidence/trade-pick-independent-review/final-focused.log). Inspected the canonical model, adapter transport/context validation, actual caller ownership mapping, manual grading, generated-package pick filtering, and AI completion boundaries. `git diff --check` passed on the correction.

Independently replayed Chrome 320×740, 390×844, and 844×390 fixtures against the actual pick hook, `TcTradeSide`, `TcVerdictPanel`, and exact reviewed shared module. All passed initial 503/retry, preserved selection after failed refresh, disabled stale rows, retry, consumed selected pick/removal, reload, seasonal unpriced rows, mounted account invalidation and horizontal bounds. [Browser results](evidence/trade-pick-independent-review/browser-result.json). Visually inspected the 320px stale/retry screenshot. This fixture replaces the surrounding player/trade engines; it is not a full authenticated Trade Center, real provider, physical-device, or completed saved-deal journey. The source owner reran the three configurations after the follow-up; this review did not duplicate that final browser run.

## Reproduced findings and corrections

1. **Ready evidence outlived the account that loaded it.** After loading A rights, a same-document write replacing `fw_session_v1` with B left the old evidence `ready`, `owns(...)` true, and `selectionIssue(...)` null. Pending-response checks alone did not protect synchronous operations on already-loaded state. The correction privately binds ready evidence and inventory to the captured account, rechecks inventory/ownership/pricing/evaluation, and makes an observed identity mismatch sticky. Actual regression includes A→B without a storage event and return of A bytes after invalidation.
2. **Conflicting MFL identifiers sent the wrong credential context.** A selected `mfl_7_2026` with `_mflLeagueId=7` but explicit credential `leagueId=999` started both draft requests for league 7 with the other connection's fixture key. The correction validates every supplied league/year identifier before either provider call and binds ready evidence to the credential context. Exact reproduction now rejects before any calls.
3. **A delayed AI completion could save A analysis while B was current.** Executing the original actual `requestAlexVerdict` with a controlled pending response reproduced both stale UI publication and `saveAIAnalysis` invocation after the account changed. The touched evaluator now captures the component account, rechecks before request and after completion, suppresses stale success/error publication and cloud-save invocation, and guards feedback entry. The actual-function regression confirms zero stale saves/text updates, while ordinary same-account completion still saves once. This does not certify the internal provider/save implementations or unrelated saved-pipeline operations.

[Reproduction source](evidence/trade-pick-independent-review/reproduce.cjs), [original observations](evidence/trade-pick-independent-review/before.log), [corrected observations](evidence/trade-pick-independent-review/after.log), [frozen original AI function reproduction](evidence/trade-pick-independent-review/ai-before.cjs). All credentials and analysis in these reproductions are explicit local fixture values.

## Preserved behavior and remaining limits

The adapter delegates remaining rights to canonical `App.buildPicksByOwner`, translates roster IDs to owner IDs without guessing, and does not replace unknown ownership with original picks. Completed seasonal drafts have zero remaining current rights; accepted dynasty future years remain unchanged. Consumed slots are excluded. MFL future rows remain exact allocations, including years/rounds outside a generic horizon; incomplete current boards remain incomplete. Unsupported ESPN/Yahoo pick sources stay visibly unavailable rather than being sent to Sleeper. Historical saved trade records are not erased.

The actual shared pin still lacks this reviewed ownership contract until root coordinates that release. Old shared bytes deliberately produce an unavailable state, so this consumer should not be described as a working released pick feature before coordination and served-byte checks. Seasonal/startup rights are visible but unpriced; this truthfulness repair does not fulfill their missing valuation model. Global one-brain/assessment completeness, full AI advice validity, saved-pipeline quota/recovery, same-document lifecycle outside the reviewed guards, and private live MFL/provider data remain independent required work. No purchase, cloud save, provider mutation, deployment or public readiness declaration occurred in this review.

Final source-owner correction revision: `2ed0274`, following `cd85e24`.

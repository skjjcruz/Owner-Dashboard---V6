# Actual-public Empire evidence assembly

Worktree `/Users/jacobc/Projects/warroom-readiness-public-empire-evidence`, branch `codex/public-empire-evidence-20260920`, baseline `ec80420` (hub `d857648` plus independently reviewed user/roster follow-up). This is a bounded repair checkpoint, not Empire launch readiness. No deployment, provider/account mutation or shared pin change.

## Reproduction and correction

**HIGH, reproduced:** actual prior `app.js` Empire bootstrap overwrote an already active league's rosters with cross-league rosters deduplicated by `roster_id`, requested an ESPN ID from Sleeper's traded-pick endpoint, and continued transaction follow-ups after a held account A DNA response completed under B. `node tests/public-empire-baseline.cjs` executes the actual baseline callback with controlled boundaries. [Reproduction](evidence/public-empire/baseline.json).

`js/public-empire.js` now assembles cloned per-league evidence; it never writes `S`, global league/user/roster/stat bridges, or global LeagueIntel. Every asynchronous boundary checks the exact hub identity snapshot and the active generation. Requests are bounded, obsolete work cannot publish or start subsequent calls, and the app rejects old-account retry/league-entry actions. Leaving Empire, entering a league, refreshing or changing the loaded league set cancels the prior caller generation. Public player metadata remains shared; private state is never cached by this coordinator.

The existing reviewed `TradePickInventory` adapter supplies league-bound ownership and draft progress. Missing/failed/unsupported/incomplete rights remain unavailable, and no synthetic three-year own-pick inventory is created by the Empire model. The canonical model's original-owner slots distinguish consumed from transferred rights. Explicit empty verified rights remain distinct from unavailable rights. Seasonal/startup rights can be shown, with their values unavailable; accepted dynasty pick prices are preserved. Global roster assessment fallback is removed: another league's roster 1 cannot supply this league's health or tier.

The coordinator fetches each league's explicit season once per run, transforms raw stats with existing `App.calcRawPts` and that league's scoring, and preserves actual games played. A successful empty current-season response may use the existing prior-season basis, explicitly labeled historical with both years. Malformed data/outages do not become empty successful seasons and never trigger that fallback. Missing roster player fields or stale roster cards cannot produce fresh assessments. Missing records remain unknown instead of becoming invented 0–0 records. The headline says partial data instead of Live when evidence is incomplete, and an absent value engine shows unavailable rather than a zero portfolio value. Cross-league exposure is explicitly a percentage of loaded owned rosters; partial list coverage stays visible across Empire views.

Saved DNA is read using a fixed current-principal database client and owner filter without calling the legacy `OD.loadDNA`, which writes an unscoped local cache after its await. No source data or legacy cache is erased. Sleeper transaction history uses the explicit league ID, checks each weekly response, and does not use `WrTxns`' implicit global season or error-to-empty cache. A failed week cannot be inferred as a complete empty transaction history. Non-Sleeper leagues never call Sleeper league endpoints; verified provider transaction metadata is respected, including ESPN's ready/stale/unavailable contract.

## Dependencies and tests

The consumer requires the already reviewed/integrated public Trade adapter batch `cd85e24` + `2ed0274`; root's combined public candidate already has it. This isolated branch does not duplicate that source or alter its canonical pin. The existing `trade` module group loads the adapter before evidence assembly. Actual rights require the reviewed canonical capital contract (`4d6ef17`, incorporated into root's shared union); an older installed shared source without `coverage` yields explicit unavailable rights. No legacy fallback fabricates picks.

- **14 focused groups** execute the real coordinator, current app bootstrap callback and Empire model: same roster IDs in two leagues; no `S`/storage mutation; exact scoring; distinct current/historical/outage states; missing/stale roster data; failed rights; non-Sleeper provider boundary and stale transaction status; account changes during players/cloud reads; request-generation interruption; timeouts; absent engine; global-assessment rejection; actual reviewed adapter consumed/transferred-rights failure/retry. [Focused log](evidence/public-empire/focused.log).
- Actual **EmpireDashboard**, coordinator, reviewed Trade adapter and actual reviewed shared pick model run in Chrome at **320×740, 390×844, 844×390**. Initial ownership failure has no phantom capital; 1/2 loaded coverage remains visible; retry recovers the completed draft's 2027–2029 rights; the league action hands off ID `111`; active `S.currentLeagueId` remains unchanged; account interruption blocks follow-ups; no horizontal overflow. [Result](evidence/public-empire/result.json), [browser log](evidence/public-empire/browser.log), [320px failure](evidence/public-empire/320-unavailable.png), [320px recovery](evidence/public-empire/320-recovered.png). The network gate permits local static GETs and controlled Sleeper GET fixtures only. Account data and assessment scores are fixtures; the outer React harness is a controlled caller. This does not certify provider-private access, production authentication, the valuation engine, real persistence or downstream league tools.
- Existing **198 checks** pass; hub **16 focused groups** pass; preview build uses exact actual-public shared `dedbb161` (44 shared modules/three data files/70 compiled scripts); focused ESLint has no warnings/errors. [Broad](evidence/public-empire/broad.log), [hub](evidence/public-empire/hub-focused.log), [build](evidence/public-empire/build.log). `npm run test:public-empire` runs the new focused/browser checks after preview build, with documented shared/adapter fixture sources.
- The first broad run correctly failed an obsolete assertion expecting twelve invented picks from a traded-pick array alone. Its failure log is retained. The replacement asserts no assumed capital without verified rights; positive consumed/transferred/current/completed behavior is covered by the real adapter tests and browser recovery, rather than a copy of implementation arithmetic.

## Open acceptance gates and next executable work

1. **HIGH, next canonical lane: cold-start valuation.** Existing shared `loadLeagueIntel()` has no explicit-context/cancellation API. It reads the global active bridge, captures active helpers, publishes/caches LI after many awaits, and starts one-brain follow-up. This consumer now avoids corrupting another league to start it. Already loaded accepted one-league-proxy values still work and are unchanged; cold-start Empire value/health loading remains incomplete, with an honest message. Root assigned the next isolated shared branch from reviewed shared union `f72c8e4` (or newer session union): reproduce global/context corruption, add the smallest explicit context/cancellation contract, preserve original market engine/injury/one-brain rules, and integrate only after independent review.
2. **HIGH, remaining advice semantics:** seasonal player valuation/health/needs, MFL future-right assessment injection, loaded one-league proxy calibration, actual one-brain per-league coherence, complete provider coverage and unknown selected franchises need additional evidence. The current reader only invokes the existing dynasty assessment path with verified Sleeper rights, normalized stats and loaded LI. No formula or owner engine decision was changed.
3. **HIGH, saved local DNA ownership:** old `od_owner_dna_v1_<league>` data has no account provenance. It is preserved but cannot safely be merged into another account's cloud notes. Account-scoped migration/recovery of legacy saved notes remains required; the coordinator's cloud read is checked and cannot write stale local results. Other existing Empire pipeline/grudge/snapshot/AI helpers and `OD.loadDNA` callers are outside this scope and still require account/persistence review.
4. Empty/non-ready values must stay visible as incomplete during integration. Root must merge current ESPN/app changes, retain pending deep links and all helpers, run the combined candidate against the intended shared union, complete independent review and then verify served revision/assets/journeys. No launch claim, live release, physical-device, native build or store claim is made by these fixture tests.

Resume from this checkpoint; do not restart the hub audit. Next is independent consumer review, then the explicit-context canonical engine lane already assigned by root.

## Independent root review and correction

Root reproduced two pending trades incorrectly classifying an owner as ACCEPTOR through the actual inference function, and found unavailable rights displayed as zero picks inside League Portfolio. The correction only passes completed Sleeper transactions to behavioral inference, rejects malformed weekly rows as unavailable, and preserves completed-trade inference. Both pick metrics in the detail view now show unknown/unavailable until ownership is verified. This follows the [documented Sleeper transaction status](https://docs.sleeper.com/#get-transactions); pending offers are not completed transactions.

The cloud DNA reader also now uses the canonical signed-owner resolver rather than choosing an arbitrary cached display username. A missing or mismatched owner cannot issue the cloud query. The independently reviewed shared session union supplies the existing resolver; its legacy-principal checks were separately covered in the full shared-module suite.

Root reran17 actual-source groups,198 broad checks and the real Empire fixture at320/390/844 with new detail-view unknown assertions. All pass. Evidence: independent-status-before.log (actual original pending inference failure), independent-status-after.log, independent-broad.log, independent-build.log and independent-browser.log under evidence/public-empire/. The first new browser assertion expected title case while CSS renders uppercase; independent-browser-label-original.log preserves that harness failure. The corrected assertion targets the metric value directly and still requires Unknown. No product change was made for the CSS label.

The bounded consumer changes are ready for combined integration. Global engine provenance/cold-start, seasonal assessment semantics and legacy local DNA migration remain explicit open lanes; these corrections do not make Empire launch-ready.

# Actual-public ESPN transaction recovery — 2026-09-20

Status: paired local consumer candidate, pending independent review and shared release coordination. Based on actual-public `f43f5ef`, paired with shared producer `1c2043e` on `d45b5f5`. No `app.js`, business model, provider settings, canonical pin, saved user data, backend or release destination changes were made.

The actual league publication path now carries league/season-checked `transactionStatus` alongside the feed into React and `window.S`. Missing, foreign or malformed ESPN status is unavailable; the consumer never interprets old producer bytes as confirmed empty. Untagged global `LI.tradeHistory` cannot fill an ESPN failure or mingle another league's history. Other providers retain their prior merge behavior. A partial background hydration applies truthful roster/feed status but rejects the full-sync promise, preserving the distinction from successful complete refresh.

The actual dashboard ticker and details show unavailable versus stale versus confirmed empty data. A single-flight, account-guarded retry invokes the existing league load path; confirmed data is retained by the scoped provider, while failed responses never erase it. The last-confirmed timestamp is shown for stale rows. Other current league tabs display the feed's uncertainty and recovery; the Free Agency ticker correctly calls this an completed ESPN trade feed rather than promising adds/drops/waivers. Retry and close controls meet a 44px minimum. No scores or events are invented.

Root's independent review caught pending/rejected/unknown records appearing as completed trades, plus the mapper's old `sides` array hiding assets from shared helpers. The coordinated producer now emits only executed trades, labels its scope `executed_trades`, and counts excluded trade records. This consumer requires that scope and rejects non-complete rows, visibly explains exclusions, and labels a confirmed empty response "No completed trades returned by ESPN." The mapper's received sides now match the keyed shared contract. Actual mapper → publication → ticker and actual Trade Center helper regressions cover both findings.

## Checks actually run

- **10 actual caller/UI groups**: publication of outage, ready/stale/empty distinctions, malformed/foreign metadata, legacy non-ESPN compatibility, actual status component copy, and actual retry callback duplicate/account guard, recovery-loader isolation from unrelated private jobs, actual mapper-to-ticker executed-only history, and actual Trade Center received/sent asset helpers. [Log](evidence/espn-transactions/caller.log).
- **Chrome 320×740, 390×844, 844×390**: actual shared provider → actual publication block and retry callback → actual ticker/detail methods. Initial503 is not empty success; retry displays normalized player/owner fields; a failed refresh keeps labeled stale rows; a verified empty retry replaces them; details open/close; reload has no fabricated cached success; no horizontal overflow. [Results](evidence/espn-transactions/result.json), [log](evidence/espn-transactions/browser.log), [320 stale](evidence/espn-transactions/320-stale.png), [short landscape](evidence/espn-transactions/844-stale.png). The surrounding league shell and external provider are controlled fixtures with all other external requests blocked. This is not a full authenticated LeagueDetail or physical-device/native journey.
- **16 existing ESPN hub/source groups** pass. [Log](evidence/espn-transactions/espn-hub.log).
- **198 existing npm tests** pass. [Log](evidence/espn-transactions/npm-test.log).
- Explicit shared source `dhq-shared-readiness-espn-transactions` preview build passes:44 shared modules,3 data files,70 compiled scripts. [Log](evidence/espn-transactions/build.log). No vendored artifacts are committed.
- ESLint: zero errors, two existing unused-variable warnings (`faColPreset`, `buildDhqStarterSet`). [Log](evidence/espn-transactions/lint.log). No test assertions or coverage removed.

## Readiness limits and next step

Release this only with the reviewed producer metadata/cache/normalization contract; no pin has been advanced here. Consumers that receive older shared bytes intentionally show unavailable instead of implying an empty verified feed. The Trade Center's current Trade Log independently reads LI history or a raw Sleeper fallback, not this ESPN feed; its ESPN integration remains an explicit open consumer path. This batch does not certify every transaction-dependent AI/assessment/FAAB/history calculation or promise full provider-history completeness. Other providers' outage states remain separate. Real ESPN access, the full league/product journey, actual served assets and post-release checks are still required. No deployment, purchase or real provider action occurred.

Independent review: root reran the original producer/boundary/caller checks and raised the two issues corrected above. Final correction spot-review is pending. Next executable step: finalize root review of paired commits, integrate the exact shared bytes with the actual-public consumer candidate, then rerun the integrated release gates. Do not label the entire ESPN experience ready from these bounded fixtures.
